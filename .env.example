# bitkol 非密配置示例（按需复制为 .env 修改）
# .env 已在 .gitignore 中忽略
# 注：本项目不涉及 LLM key（Agent 自身负责 LLM 推理，脚本不调用任何 LLM API）

# Nitter 镜像列表（逗号分隔，按优先级）。也可在 skills/kol_briefing/config.toml 的 [source.nitter].mirrors 配置
# .env 优先级高于 config.toml（若设置）
NITTER_MIRRORS=https://nitter.net,https://nitter.poast.org,https://nitter.privacydev.net
